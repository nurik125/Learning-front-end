// Code an arrow function that adds 10% to each value in the prices array.

// const prices = [1.99, 2.49, 0.99, 4.99];

//                        :1:Method

// const taxed = prices.reduce((prev, curr, index) => {
//   if (index == 1) {
//     return prev * 0.1 + curr * 0.1;
//   }
//   return prev + curr * 0.1;
// });

// console.log(taxed);

//                    :2:Method

// const prices = [1.99, 2.49, 0.99, 4.99];

// const taxed = prices.map((element) => {
//   return element * 0.1;
// });

// console.log(taxed.reduce((prev, curr) => prev + curr));

